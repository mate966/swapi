export type ContactFormProps = {
    block: {
        title: string;
        description: string;
        form: {
            name: string;
            email: string;
            message: string;
        };
    };
};
